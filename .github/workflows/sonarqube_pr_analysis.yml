# DISABLED - To re-enable, remove the 'on: workflow_dispatch' line and uncomment the original 'on:' trigger below
on: workflow_dispatch  # This disables automatic triggers; workflow can only be run manually

# Original triggers (commented out to disable):
# on:
#   push:
#     branches:
#       - feature/payment_provider_gateway
#       - sonarqube-refactor-fixes
#   pull_request:
#     types: [opened, synchronize, reopened]

name: SonarQube PR Analysis (DISABLED)
jobs:
  sonarqube:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
